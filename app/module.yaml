product: jvm/app

dependencies:
  - ../lib
  - $libs.ajalt.clikt

test-dependencies:
  - $libs.dachlatten.io

settings:
  publishing:
    artifactId: opml-tool
  kotlin:
    optIns:
      - kotlin.time.ExperimentalTime

apply:
  - ../publish.module-template.yaml
